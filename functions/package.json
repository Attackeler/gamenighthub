{
  "name": "gamenighthub-functions",
  "description": "Cloud Functions for BoardGameGeek proxy/cache",
  "main": "lib/index.js",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts src",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "shell": "firebase functions:shell"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.0",
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.5.0",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
