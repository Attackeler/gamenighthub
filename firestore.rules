rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary permissive rules for development. Update with proper auth
    // checks before shipping to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
